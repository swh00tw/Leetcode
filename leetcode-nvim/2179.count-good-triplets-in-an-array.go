package main

// @leet start
func goodTriplets(nums1 []int, nums2 []int) int64 {
	// need hashmap for each arr to store val to idx
	// use 2 for loops to find the first two nums,
	// count += the shared nums on the right side of num2 (need to build a hashmap for this)
}

// @leet end

